# SW Plumbing image assets

Drop the real AI-generated photos here using these EXACT filenames. Until a file
exists, the site shows a labelled blue placeholder in its place (no broken images).

| Filename | Used in |
|---|---|
| `hero-plumber-home.jpg` | Hero — big image card (top-right) |
| `hero-emergency-video.mp4` | Emergency CTA — looping muted background video (optional) |
| `team-plumbers.jpg` | "Why choose SW" — large team photo |
| `blocked-drain.jpg` | Services grid + footer strip |
| `hot-water.jpg` | Services grid + footer strip |
| `leaking-tap.jpg` | Services grid |
| `leaking-pipe.jpg` | Services grid |
| `gas-fitting.jpg` | Services grid + footer strip |
| `emergency-plumber.jpg` | Services grid + Emergency CTA (video poster) + footer strip |
| `plumber-homeowner.jpg` | (spare — swap into any section) |
| `brisbane-service-van.jpg` | Local / Brisbane split section |
| `testimonial-homeowner.jpg` | Reviews — side image |

Recommended: JPG/WEBP, ~1600px wide, landscape (except the hero which suits
portrait/4:5). Get watermark-free exports from Higgsfield before launch.
