---
import Layout from '../layouts/Layout.astro';
import Section from '../components/Section.astro';
import SectionHeading from '../components/SectionHeading.astro';
import ContactForm from '../components/ContactForm.astro';
import MapPanel from '../components/MapPanel.astro';
import Accordion from '../components/Accordion.astro';
import CtaBand from '../components/CtaBand.astro';
import CallButton from '../components/CallButton.astro';
import Icon from '../components/Icon.astro';
import { business, faqs } from '../data/site';

const details = [
  { icon: 'phoneline', label: 'Phone', value: business.phone, sub: 'Tap to call now', href: business.phoneHref },
  { icon: 'facebook', label: 'Facebook', value: 'Message us', sub: 'S & W Plumbing', href: business.facebook },
  { icon: 'clock', label: 'Hours', value: 'Call to arrange', sub: 'Local response where available' },
  { icon: 'pin', label: 'Service area', value: 'Birkdale & the bay', sub: 'Bayside Brisbane & Redlands' },
];

const trust = ['Clear pricing before work starts', 'Clean, tidy work', 'Local to Birkdale', 'Call for the fastest time'];

const faqSchema = [
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faqs.map((f) => ({
      '@type': 'Question',
      name: f.q,
      acceptedAnswer: { '@type': 'Answer', text: f.a },
    })),
  },
];
---
<Layout
  title="Contact S&W Plumbing | Birkdale & Bayside Brisbane Plumber"
  description="Call S&W Plumbing on 0432 292 371 for local plumbing across Birkdale, Bayside Brisbane and the Redlands."
  schema={faqSchema}
>
  <!-- ===== HERO ===== -->
  <section class="chero">
    <canvas id="swp-hero-canvas" class="chero__canvas"></canvas>
    <div class="chero__veil"></div>
    <div class="chero__inner">
      <div data-hero class="badge">
        <span class="badge__dot"></span>
        <span>Get in touch · Bayside Brisbane</span>
      </div>
      <h1 class="chero__title hero-mask">
        <span class="reveal-line"><span>Need a plumber?</span></span>
        <span class="reveal-line"><span>Let's get it <em class="kinetic">sorted.</em></span></span>
      </h1>
      <p data-hero class="chero__sub">Call now for the fastest response, or send the details and we'll get back to you.</p>
      <div data-hero class="chero__cta">
        <CallButton size="lg" pulse />
        <a href="#contact-form" class="chero__secondary">Send enquiry</a>
      </div>
    </div>
  </section>

  <!-- ===== QUICK DETAILS ===== -->
  <Section border={false} class="details-section">
    <div class="details" data-anim-children>
      {
        details.map((d) => {
          const Tag = d.href ? 'a' : 'div';
          return (
            <Tag href={d.href} class="detail" data-tilt>
              <div class="detail__icon"><Icon name={d.icon} size={25} stroke="#00A651" /></div>
              <div class="detail__label">{d.label}</div>
              <div class="detail__value">{d.value}</div>
              <div class="detail__sub">{d.sub}</div>
            </Tag>
          );
        })
      }
    </div>
  </Section>

  <!-- ===== FORM + MAP ===== -->
  <Section tint>
    <div class="contact-grid">
      <div data-reveal id="contact-form">
        <div class="eyebrow">Send a message</div>
        <h2 class="h2 contact__h2">Send the job details.</h2>
        <p class="contact__lead">Tell us what's happening, where you are, and the best number to call.</p>

        <ul class="trust-strip" aria-label="How we work">
          {trust.map((t) => <li><Icon name="check" size={15} stroke="#00A651" /> {t}</li>)}
        </ul>

        <ContactForm />
      </div>

      <div data-reveal>
        <div class="eyebrow">Where we're based</div>
        <h2 class="h2 contact__h2">Birkdale, QLD.</h2>
        <div class="contact__map"><MapPanel variant="mini" /></div>
        <p class="contact__map-note">Based near Birkdale and servicing Bayside Brisbane, the Redlands and nearby suburbs.</p>
      </div>
    </div>
  </Section>

  <!-- ===== FAQ ===== -->
  <Section>
    <div class="faq-wrap">
      <SectionHeading eyebrow="Good to know" max="820px">Quick questions.</SectionHeading>
      <Accordion items={faqs} />
    </div>
  </Section>

  <CtaBand>
    One call and<br />it's <span class="lime">sorted.</span>
  </CtaBand>
</Layout>

<style>
  /* HERO */
  .chero {
    position: relative;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 132px var(--pad-x) 68px;
    color: #fff;
    background:
      radial-gradient(120% 90% at 50% 0%, rgba(0, 166, 81, 0.16), transparent 55%),
      linear-gradient(160deg, var(--navy) 0%, var(--navy-2) 100%);
  }
  .chero__canvas,
  .chero__veil {
    display: none;
  }
  .chero__veil {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: radial-gradient(120% 100% at 50% 0%, rgba(255, 255, 255, 0) 36%, rgba(255, 255, 255, 0.92) 100%);
  }
  .chero__inner {
    position: relative;
    z-index: 2;
    max-width: 900px;
    width: 100%;
    margin: 0 auto;
    text-align: center;
  }
  .chero__title {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(2.8rem, 9vw, 6.4rem);
    line-height: 0.95;
    letter-spacing: -0.025em;
    margin: 0 auto;
    max-width: 13ch;
    color: #fff;
  }
  .chero__title :global(em) {
    font-style: normal;
    color: var(--orange);
  }
  .chero__sub {
    margin: 24px auto 0;
    font-size: clamp(1.1rem, 2.4vw, 1.45rem);
    color: rgba(255, 255, 255, 0.82);
    max-width: 34ch;
    line-height: 1.5;
    font-weight: 500;
  }
  .chero__cta {
    margin-top: 40px;
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    justify-content: center;
  }
  .chero__secondary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    border: 1.5px solid rgba(255, 255, 255, 0.55);
    color: #fff;
    font-weight: 700;
    font-size: 1.05rem;
    padding: 15px 26px;
    border-radius: 999px;
    transition: border-color 0.25s, background 0.25s;
  }
  .chero__secondary:hover {
    border-color: #fff;
    background: rgba(255, 255, 255, 0.1);
  }
  .chero__note {
    margin-top: 18px;
    font-size: 13.5px;
    color: rgba(255, 255, 255, 0.7);
  }
  .badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 7px 14px;
    border: 1px solid rgba(255, 255, 255, 0.16);
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
    border-radius: 999px;
    margin-bottom: 28px;
    font-size: 12.5px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #fff;
    font-weight: 600;
  }
  .badge__dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 10px var(--lime);
  }

  /* DETAILS */
  .details {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 16px;
  }
  .detail {
    display: block;
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 30px;
    transition: transform 0.25s var(--ease-quint), border-color 0.4s, box-shadow 0.4s;
    transform-style: preserve-3d;
  }
  .detail:hover {
    border-color: rgba(0, 166, 81, 0.22);
  }
  .detail__icon {
    width: 50px;
    height: 50px;
    border-radius: 13px;
    background: var(--lime-10);
    border: 1px solid rgba(0, 166, 81, 0.22);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 22px;
    color: var(--lime);
  }
  .detail__label {
    font-size: 12px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--lime-dark);
    font-weight: 700;
    margin-bottom: 8px;
  }
  .detail__value {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.3rem;
    letter-spacing: -0.01em;
    word-break: break-word;
  }
  .detail__sub {
    font-size: 13.5px;
    color: var(--muted-2);
    margin-top: 6px;
  }

  /* FORM + MAP */
  .contact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 40px;
    align-items: start;
  }
  .contact__h2 {
    font-size: clamp(1.9rem, 4.4vw, 3rem);
    margin: 16px 0 12px;
  }
  .contact__lead {
    font-size: 14.5px;
    color: var(--muted-2);
    line-height: 1.55;
    margin-bottom: 22px;
  }
  .trust-strip {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 10px 18px;
    margin-bottom: 28px;
  }
  .trust-strip li {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-size: 13.5px;
    font-weight: 600;
    color: var(--text);
  }
  .contact__map {
    margin-top: 8px;
  }
  .contact__map-note {
    font-size: 13.5px;
    color: var(--muted-2);
    margin-top: 16px;
    line-height: 1.5;
  }

  .faq-wrap {
    max-width: 820px;
    margin: 0 auto;
  }
</style>
